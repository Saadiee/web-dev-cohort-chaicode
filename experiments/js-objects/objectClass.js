class Person{                           // class is blueprint for object
    constructor(fname,lname){           // constructor includes all those properties that we want in that object
        this.fname = fname;
        this.lname = lname;
    }
}